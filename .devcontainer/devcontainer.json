
{
  "name": "Python 3.11.3",
  "image": "python:3.11.3-bullseye",
  "postCreateCommand": "apt-get update && apt-get install -y python3-pip && pip3 install --upgrade pip && pip3 install --user pipenv"
}

